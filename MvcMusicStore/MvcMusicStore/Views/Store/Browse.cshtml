@model MvcMusicStore.Models.Gen

@{
    ViewBag.Title = "Przeglądaj";
}

<h2>Przeglądaj gatunki: @Model.Name</h2>
<ul>
    @foreach (var album in Model.Albums)
    {
        <li>
            @Html.ActionLink(album.Title,
     "Szczegóły", new { id = album.AlbumId })
        </li>
    }
</ul>


